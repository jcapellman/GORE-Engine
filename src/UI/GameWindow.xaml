<Window
    x:Class="GORE.UI.GameWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Background="#000000" KeyDown="RootGrid_KeyDown" KeyUp="RootGrid_KeyUp" Loaded="RootGrid_Loaded">
        <!-- 3D Viewport (fills entire window) -->
        <Image x:Name="ViewportImage" Stretch="Fill"/>

        <!-- HUD Overlay - Bottom Left (Health) -->
        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Left" 
                    VerticalAlignment="Bottom"
                    Margin="30,0,0,30">
            <Border Background="#330000" 
                    BorderBrush="#FF0000" 
                    BorderThickness="3" 
                    Padding="15,5"
                    CornerRadius="3">
                <TextBlock x:Name="HealthText" 
                          Text="100"
                          Foreground="#00FF00"
                          FontFamily="Consolas"
                          FontSize="48"
                          FontWeight="Black"
                          VerticalAlignment="Center"/>
            </Border>
            <TextBlock Text="HEALTH"
                      Foreground="#888888"
                      FontFamily="Consolas"
                      FontSize="16"
                      FontWeight="Bold"
                      VerticalAlignment="Center"
                      Margin="10,0,0,0"/>
        </StackPanel>

        <!-- HUD Overlay - Bottom Right (Ammo) -->
        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Bottom"
                    Margin="0,0,30,30">
            <TextBlock Text="AMMO"
                      Foreground="#888888"
                      FontFamily="Consolas"
                      FontSize="16"
                      FontWeight="Bold"
                      VerticalAlignment="Center"
                      Margin="0,0,10,0"/>
            <Border Background="#331100" 
                    BorderBrush="#FFAA00" 
                    BorderThickness="3" 
                    Padding="15,5"
                    CornerRadius="3">
                <TextBlock x:Name="AmmoText" 
                          Text="50"
                          Foreground="#FFFF00"
                          FontFamily="Consolas"
                          FontSize="48"
                          FontWeight="Black"
                          VerticalAlignment="Center"/>
            </Border>
        </StackPanel>

        <!-- FPS Counter - Top Right -->
        <TextBlock x:Name="FpsText" 
                  Text="60fps"
                  Foreground="White"
                  FontFamily="Consolas"
                  FontSize="16"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Top"
                  Margin="0,10,10,0"/>

        <!-- Quake 3 Style Console Overlay -->
        <Grid x:Name="ConsoleOverlay" 
              VerticalAlignment="Top"
              Height="400"
              Visibility="Collapsed"
              RenderTransformOrigin="0.5,0">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="ConsoleTransform" Y="-400"/>
            </Grid.RenderTransform>

            <!-- Console Background -->
            <Border Background="#DD000000" 
                    BorderBrush="#FF8800" 
                    BorderThickness="0,0,0,3">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Console History -->
                    <ScrollViewer x:Name="ConsoleScrollViewer" 
                                  Grid.Row="0" 
                                  VerticalScrollBarVisibility="Auto"
                                  Padding="10">
                        <TextBlock x:Name="ConsoleHistoryText"
                                   FontFamily="Consolas"
                                   FontSize="14"
                                   Foreground="#00FF00"
                                   TextWrapping="Wrap"/>
                    </ScrollViewer>

                    <!-- Console Input -->
                    <Grid Grid.Row="1" Background="#11000000" Padding="10,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="]" 
                                   Grid.Column="0"
                                   FontFamily="Consolas"
                                   FontSize="16"
                                   Foreground="#FF8800"
                                   Margin="0,0,5,0"
                                   VerticalAlignment="Center"/>

                        <TextBox x:Name="ConsoleInput"
                                 Grid.Column="1"
                                 FontFamily="Consolas"
                                 FontSize="16"
                                 Foreground="#FFFFFF"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 KeyDown="ConsoleInput_KeyDown"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>