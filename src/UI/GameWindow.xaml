<Window
    x:Class="GORE.UI.GameWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid" Background="#000000" KeyDown="RootGrid_KeyDown" KeyUp="RootGrid_KeyUp" Loaded="RootGrid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 3D Viewport -->
        <Border Grid.Row="0" BorderBrush="DarkGray" BorderThickness="2">
            <Image x:Name="ViewportImage" Stretch="Fill"/>
        </Border>

        <!-- HUD -->
        <Grid Grid.Row="1" Background="#1a1a1a" Padding="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Stats -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="20">
                <TextBlock x:Name="HealthText" 
                          Text="HEALTH: 100"
                          Foreground="Red"
                          FontFamily="Consolas"
                          FontSize="20"
                          FontWeight="Bold"/>
                <TextBlock x:Name="AmmoText" 
                          Text="AMMO: 50"
                          Foreground="Yellow"
                          FontFamily="Consolas"
                          FontSize="20"
                          FontWeight="Bold"/>
            </StackPanel>

            <!-- Controls Info -->
            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                <TextBlock Text="WASD: Move | ARROWS: Look | SPACE: Shoot | ESC: Menu"
                          Foreground="LightGray"
                          FontFamily="Consolas"
                          FontSize="14"
                          HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Position Debug -->
            <TextBlock x:Name="PositionText" 
                      Grid.Column="2"
                      Text="X: 0.0 Y: 0.0"
                      Foreground="Cyan"
                      FontFamily="Consolas"
                      FontSize="16"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
